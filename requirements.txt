# Pin spaces to a version compatible with gradio==4.44.0 (which HF Spaces installs)
# spaces 0.50.4+ requires gradio==4.44.1 which conflicts with HF Spaces' gradio==4.44.0
spaces==0.30.0
diffusers>=0.24.0
transformers>=4.36.0
accelerate>=0.24.0
safetensors>=0.4.0
Pillow>=10.0.0
peft>=0.7.0 